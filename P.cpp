#include <stdio.h>
#include <math.h>

int main() {
	// freopen(".inp", "r", stdin);
	// freopen(".out", "w", stdout);
	int n;
	scanf("%d", &n);
	if (n <= 4) printf("Kem");
	else printf("Ca");
	return 0;
}